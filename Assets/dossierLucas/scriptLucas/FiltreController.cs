using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FiltreController : MonoBehaviour // Notes : les Filtres sont temporaires
{
    // Start is called before the first frame update

    /*public CameraFilterPack_3D_Distortion BossFiltre;
    public CameraFilterPack_Colors_DarkColor PauseFiltre; 
    public GameController gameController;

    // Update is called once per frame
    void Update()
    {
        ResetAllFiltre();

        if (gameController.isGameRunning)
        {
            if (gameController.currentStateOfGame == GameController.StateOfGame.Boss) // filtres si état du jeu = Boss
            {
                BossFiltre._FixDistance = 1f;
            }
        }
        else // filtre si le jeu en pause
        {
            PauseFiltre.Alpha = 1f;
        }
        
    }

    private void ResetAllFiltre() // rend transparent tous les filtres appliqués
    {
        BossFiltre._FixDistance = 0f;
        PauseFiltre.Alpha = 0f;
    }*/
}
